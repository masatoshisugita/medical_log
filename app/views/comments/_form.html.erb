<%= form_with(model: [topic, comment]) do |f| %>
  <div>
    <%= f.label :review %><span>(150文字以内)</span><br>
    <%= f.text_area :review %>
  </div>
  <div class="actions">
    <%= f.submit "コメントをする", class: "btn btn-x btn-outline-secondary" %>
  </div>
<% end %>
