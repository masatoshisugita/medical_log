
<% comments.each do |comment| %>
  <% unless comment.id.nil? %>
    <p><%= link_to "#{comment.user.name}さん",user_path(comment.user.id) %></p>
    <p><%= comment.review %></p>
    <% if comment.user == current_user %>
      <p><%= button_to "削除する",topic_comment_path(comment.topic_id,comment.id),method: :delete,class: "btn btn-danger text-right", remote: true %></p><hr size="5px" color="black">
    <% end %>
  <% end %>
<% end %>

